using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MapGrid : MonoBehaviour
{
    Vector2 size;

    void Start()
    {
        //poolManager = Locator.Resolve<PoolManager>();//pool manager is needed to spawn objects
    }
}
